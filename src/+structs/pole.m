function p = pole(opts)

    arguments
        opts.idx double = []  % pole indices
        opts.s   double = []  % pole in s coordinate
        opts.xp  double = []  % pole in xp coordinate
        opts.yp  double = []  % pole in yp coordinate
        opts.rad double = 0   % radius of curvature
    end

    p = opts;

end